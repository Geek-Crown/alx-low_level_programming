#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of a string
 * @s: string array
 *
 * Return: The length of string array
 */
int _strlen(char *s)
{
	len = strlen(s);
	Return (len);
}
